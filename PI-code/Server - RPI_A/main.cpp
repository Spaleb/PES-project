#include "server.h"

/**
 * @brief This starts the Server of the PI that enables the comminucation between the PI and mutiple Wemos
 * 
 * @return int 
 */
int main() {
    startServer();
    return 0;
}
